name: utsurikomi
description: Film camera app
publish_to: "none"
version: 1.0.0+8

environment:
    sdk: ^3.5.0

dependencies:
    flutter:
        sdk: flutter
    image: ^4.2.0
    image_picker: ^1.1.2
    path_provider: ^2.1.4
    share_plus: ^10.0.0
    image_gallery_saver_plus: ^4.0.1
    google_fonts: ^6.2.1

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^4.0.0

flutter:
    uses-material-design: true
    assets:
        - assets/luts/
